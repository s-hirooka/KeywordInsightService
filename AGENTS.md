# 作業ルール

- 回答は日本語
- C#コードは省略せず完全な形で出す
- using を省略しない
- 既存プロジェクトへ最小差分で追加する
- 破壊的変更を避ける
- Windows Forms のコントロール名は指示がある場合それに従う
- Google Ads API では historical metrics を使う
- 低単価、高単価は LowTopOfPageBid / HighTopOfPageBid とする
- DataGridView の表示内容は SQLite に保存する
- SQLite 初期化、保存、読込は分離する
- API使いすぎ防止として、上限、待機、キャッシュを必ず入れる
- エラーメッセージは日本語にする
- 最初に実装方針、フォルダ構成、追加/修正ファイル一覧を示す